# Cascade RCA Environment Variables
# Copy to .env and populate values if available.
# The application gracefully functions with deterministic fallbacks when these are unset.

GITHUB_TOKEN=
GITHUB_OWNER=
GITHUB_REPO=
GEMINI_API_KEY=
