# Cascade RCA Environment Variables
# Copy to .env and configure as needed.
# Note: The system will operate with realistic deterministic fallbacks if external keys are omitted.

GITHUB_TOKEN=
GITHUB_OWNER=
GITHUB_REPO=
GEMINI_API_KEY=
